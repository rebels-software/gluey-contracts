---
phase: 01-core-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Gluey.Contract/ParsedProperty.cs
  - src/Gluey.Contract/ValidationErrorCode.cs
  - src/Gluey.Contract/ValidationError.cs
  - src/Gluey.Contract/ValidationErrorMessages.cs
  - tests/Gluey.Contract.Tests/ParsedPropertyTests.cs
  - tests/Gluey.Contract.Tests/ValidationErrorTests.cs
autonomous: true
requirements: [CORE-01, CORE-02, CORE-04]

must_haves:
  truths:
    - "ParsedProperty can hold offset, length, and path into a byte buffer and report HasValue correctly"
    - "ParsedProperty materializes values via GetString(), GetInt32(), GetInt64(), GetDouble(), GetBoolean(), GetDecimal() from raw UTF-8 bytes"
    - "ParsedProperty.RawBytes exposes the underlying bytes as ReadOnlySpan<byte>"
    - "Type mismatch on GetX() returns default(T) without exceptions"
    - "ValidationError carries an RFC 6901 JSON Pointer path, a ValidationErrorCode, and a static message string"
    - "ValidationErrorCode enum covers all JSON Schema Draft 2020-12 keywords (~28 values) plus TooManyErrors sentinel"
    - "ValidationErrorMessages returns a pre-allocated static string for every error code"
  artifacts:
    - path: "src/Gluey.Contract/ParsedProperty.cs"
      provides: "Zero-allocation byte buffer accessor with value materialization"
      min_lines: 60
    - path: "src/Gluey.Contract/ValidationErrorCode.cs"
      provides: "Machine-readable error code enum for all JSON Schema keywords"
      min_lines: 30
    - path: "src/Gluey.Contract/ValidationError.cs"
      provides: "Readonly struct with path, code, and message"
      min_lines: 15
    - path: "src/Gluey.Contract/ValidationErrorMessages.cs"
      provides: "Static message lookup by error code"
      min_lines: 30
    - path: "tests/Gluey.Contract.Tests/ParsedPropertyTests.cs"
      provides: "Unit tests for ParsedProperty construction and materialization"
      min_lines: 50
    - path: "tests/Gluey.Contract.Tests/ValidationErrorTests.cs"
      provides: "Unit tests for ValidationError, ValidationErrorCode, and message lookup"
      min_lines: 30
  key_links:
    - from: "src/Gluey.Contract/ValidationError.cs"
      to: "src/Gluey.Contract/ValidationErrorCode.cs"
      via: "Code field typed as ValidationErrorCode"
      pattern: "ValidationErrorCode Code"
    - from: "src/Gluey.Contract/ValidationError.cs"
      to: "src/Gluey.Contract/ValidationErrorMessages.cs"
      via: "Message sourced from static lookup"
      pattern: "ValidationErrorMessages\\.Get"
---

<objective>
Implement the two foundational leaf types for the Gluey.Contract library: ParsedProperty (the zero-allocation byte buffer accessor) and the validation error types (ValidationErrorCode enum, ValidationError struct, ValidationErrorMessages static lookup).

Purpose: These are the leaf types with no internal dependencies. Every downstream type (OffsetTable, ErrorCollector, ParseResult) and every downstream phase (validation, walker) produces or consumes these types. Getting their shapes locked down first prevents layout changes later.

Output: 4 source files in src/Gluey.Contract/ and 2 test files in tests/Gluey.Contract.Tests/
</objective>

<execution_context>
@C:/Users/kmmal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kmmal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-types/01-CONTEXT.md
@.planning/phases/01-core-types/01-RESEARCH.md

<interfaces>
<!-- Existing stubs to replace -->

From src/Gluey.Contract/ParsedProperty.cs:
```csharp
namespace Gluey.Contract;
public readonly struct ParsedProperty { }
```

From tests/Gluey.Contract.Tests/GlobalUsings.cs:
```csharp
global using NUnit.Framework;
global using FluentAssertions;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ParsedProperty readonly struct with value materialization</name>
  <files>src/Gluey.Contract/ParsedProperty.cs, tests/Gluey.Contract.Tests/ParsedPropertyTests.cs</files>
  <behavior>
    - Test: default ParsedProperty has HasValue == false, Path == string.Empty
    - Test: ParsedProperty constructed with buffer/offset/length/path reports HasValue == true and correct Path
    - Test: RawBytes returns the correct byte slice
    - Test: GetString() returns UTF-8 decoded string from buffer bytes
    - Test: GetString() on empty property returns string.Empty
    - Test: GetInt32() parses integer from UTF-8 bytes (e.g., "42" bytes -> 42)
    - Test: GetInt64() parses long from UTF-8 bytes
    - Test: GetDouble() parses double from UTF-8 bytes (e.g., "3.14" bytes -> 3.14)
    - Test: GetBoolean() returns true for "true" bytes (length 4, starts with 't'), false for "false" bytes
    - Test: GetDecimal() parses decimal from UTF-8 bytes
    - Test: All GetX() methods return default(T) when HasValue is false (length == 0)
    - Test: ParsedProperty.Empty returns a default instance with HasValue == false
  </behavior>
  <action>
Replace the empty ParsedProperty stub in src/Gluey.Contract/ParsedProperty.cs with the full implementation:

1. Readonly struct with fields: byte[] _buffer, int _offset, int _length, string _path
2. Internal constructor: ParsedProperty(byte[] buffer, int offset, int length, string path)
3. Properties: Path (returns _path ?? string.Empty), HasValue (_length > 0), RawBytes (ReadOnlySpan<byte> via _buffer.AsSpan)
4. Materialization methods with [MethodImpl(MethodImplOptions.AggressiveInlining)]:
   - GetString(): Encoding.UTF8.GetString(_buffer, _offset, _length) -- allocates intentionally (per user decision)
   - GetInt32(): Utf8Parser.TryParse on span, returns value or default
   - GetInt64(): same pattern
   - GetDouble(): same pattern
   - GetBoolean(): _length == 4 && _buffer[_offset] == (byte)'t' (per research pattern)
   - GetDecimal(): same Utf8Parser pattern
5. Static property: Empty => default
6. All GetX() methods guard with if (_length == 0) return default

Per user decisions:
- NO TryGetX() variants
- Type mismatch returns default(T) -- zero branching
- GetString() allocates (outside parse path)
- RawBytes exposed for advanced users

Use System.Buffers.Text.Utf8Parser for numeric parsing (do NOT hand-roll).
Use System.Text.Encoding.UTF8 for string decoding (do NOT hand-roll).
Add XML doc comments on all public members.
  </action>
  <verify>
    <automated>dotnet test tests/Gluey.Contract.Tests --filter FullyQualifiedName~ParsedPropertyTests -q</automated>
  </verify>
  <done>ParsedProperty readonly struct compiles with all 6 GetX() methods, HasValue, Path, RawBytes, and Empty. All unit tests pass covering construction, materialization from UTF-8 bytes, and default/empty behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ValidationErrorCode enum, ValidationError struct, and ValidationErrorMessages lookup</name>
  <files>src/Gluey.Contract/ValidationErrorCode.cs, src/Gluey.Contract/ValidationError.cs, src/Gluey.Contract/ValidationErrorMessages.cs, tests/Gluey.Contract.Tests/ValidationErrorTests.cs</files>
  <behavior>
    - Test: ValidationErrorCode enum has None = 0, TypeMismatch, RequiredMissing, TooManyErrors, and all ~28 keyword values
    - Test: ValidationErrorCode is backed by byte (: byte)
    - Test: ValidationError constructed with path/code/message exposes all three fields correctly
    - Test: ValidationError is a readonly struct (verify via reflection or compile-time)
    - Test: ValidationErrorMessages.Get(ValidationErrorCode.TypeMismatch) returns a non-empty string
    - Test: ValidationErrorMessages.Get(ValidationErrorCode.TooManyErrors) returns the sentinel message
    - Test: ValidationErrorMessages.Get(ValidationErrorCode.None) returns string.Empty
    - Test: Every ValidationErrorCode value (except None) has a non-empty message in ValidationErrorMessages
  </behavior>
  <action>
Create three new files:

1. src/Gluey.Contract/ValidationErrorCode.cs:
   - public enum ValidationErrorCode : byte
   - Values per research: None=0, TypeMismatch, EnumMismatch, ConstMismatch, RequiredMissing, AdditionalPropertyNotAllowed, ItemsInvalid, PrefixItemsInvalid, MinimumExceeded, MaximumExceeded, ExclusiveMinimumExceeded, ExclusiveMaximumExceeded, MultipleOfInvalid, MinLengthExceeded, MaxLengthExceeded, PatternMismatch, MinItemsExceeded, MaxItemsExceeded, MinPropertiesExceeded, MaxPropertiesExceeded, AllOfInvalid, AnyOfInvalid, OneOfInvalid, NotInvalid, IfThenInvalid, IfElseInvalid, DependentRequiredMissing, DependentSchemaInvalid, PatternPropertyInvalid, PropertyNameInvalid, ContainsInvalid, MinContainsExceeded, MaxContainsExceeded, UniqueItemsViolation, FormatInvalid, TooManyErrors
   - Full enum defined upfront (per user decision -- no breaking changes across phases)
   - XML doc comments with grouped sections by keyword category

2. src/Gluey.Contract/ValidationError.cs:
   - public readonly struct ValidationError
   - Public readonly fields: string Path, ValidationErrorCode Code, string Message
   - Constructor: ValidationError(string path, ValidationErrorCode code, string message)
   - Per user decision: path is pre-allocated string from schema (invariant 3), message is static compile-time string
   - XML doc comments on all public members

3. src/Gluey.Contract/ValidationErrorMessages.cs:
   - internal static class ValidationErrorMessages
   - Private static readonly string[] Messages array indexed by (int)ValidationErrorCode
   - Static constructor populates all entries with descriptive static strings
   - Public static method: string Get(ValidationErrorCode code) => Messages[(int)code] ?? string.Empty
   - Per user decision: NO string interpolation, NO runtime context -- static compile-time messages only

4. tests/Gluey.Contract.Tests/ValidationErrorTests.cs:
   - Test construction and field access
   - Test every enum value has a corresponding message
   - Test None returns empty string
   - Test TooManyErrors sentinel message
  </action>
  <verify>
    <automated>dotnet test tests/Gluey.Contract.Tests --filter FullyQualifiedName~ValidationErrorTests -q</automated>
  </verify>
  <done>ValidationErrorCode enum has ~35 values (byte-backed), ValidationError readonly struct holds path/code/message, ValidationErrorMessages provides static lookup for all codes. All tests pass including coverage check that every code has a message.</done>
</task>

</tasks>

<verification>
dotnet build src/Gluey.Contract/Gluey.Contract.csproj -q
dotnet test tests/Gluey.Contract.Tests --filter "FullyQualifiedName~ParsedPropertyTests|FullyQualifiedName~ValidationErrorTests" -q
</verification>

<success_criteria>
- ParsedProperty readonly struct with internal constructor, HasValue, Path, RawBytes, 6 GetX() methods, Empty
- ValidationErrorCode enum with ~35 byte-backed values covering all JSON Schema keywords
- ValidationError readonly struct with Path, Code, Message fields
- ValidationErrorMessages internal static class with Get() method returning static strings
- All unit tests green
- No heap allocations in GetX() methods except GetString() (by design)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-types/01-01-SUMMARY.md`
</output>
